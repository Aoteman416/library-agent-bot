# ===== 图书馆智能客服 - 依赖清单 =====
# 安装：
#   pip install -r requirements.txt
#   pip install -r requirements.txt -r requirements-rag.txt   （接入 BGE + Chroma + Cross-Encoder 重排序）
# 注：requirements-rag.txt 含 torch/chromadb，体积较大；不装也能启动（自动降级稀疏检索）。

# ---- 核心依赖（启动必装）----
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
httpx>=0.27
python-dotenv>=1.0

# ---- 鉴权 / 安全（JWT + Refresh Token，对应 v2 9.1）----
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7.4

# ---- 缓存（语义缓存 Redis 后端，对应 v2 9.1）----
redis>=5.0.3

# ---- 关系数据库（users/sessions/messages 等，对应 v2 5.1）----
sqlalchemy>=2.0
pymysql>=1.1.0

# ---- 上传解析（知识库文档 .pdf/.docx，对应 v2 4.2.4）----
pypdf>=4.2.0
python-docx>=1.1.0
python-multipart>=0.0.9

# ---- 日志 ----
loguru>=0.7.2

# ---- 测试与评测 ----
pytest>=8.1
