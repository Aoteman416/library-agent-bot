# 图书馆智能客服系统 - 需求分析与技术设计

> 版本：v2.0 | 面向：AI面试备考 + 实际项目开发
> 核心技术栈：**FastAPI + RAG (Retrieval-Augmented Generation) + 向量数据库**

---

## 一、系统概述

### 1.1 项目定位
基于 RAG（检索增强生成）架构的图书馆智能客服系统，通过 FastAPI 提供 RESTful API，结合向量数据库实现知识库语义检索，为用户提供7×24小时的图书馆服务咨询。

### 1.2 核心价值
- **降本增效**：减少馆员重复性工作，提升服务效率
- **精准服务**：基于知识库+LLM的精准回答，降低幻觉率
- **数据驱动**：通过对话记录分析，持续优化服务质量

### 1.3 技术架构图（文字版）

```
┌─────────────────────────────────────────────────────────────────┐
│                        客户端层                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐   │
│  │  Web 前端    │  │  微信小程序  │  │  触屏一体机/自助终端  │   │
│  └──────┬───────┘  └──────┬───────┘  └──────────┬───────────┘   │
└─────────┼──────────────────┼─────────────────────┼──────────────┘
          │                  │                     │
          ▼                  ▼                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                      API 网关层 (FastAPI)                        │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  认证中间件 │ 限流中间件 │ 日志中间件 │ CORS中间件        │    │
│  └─────────────────────────────────────────────────────────┘    │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌─────────────┐  │
│  │ 对话接口   │ │ 业务接口   │ │ 管理接口   │ │ WebSocket  │  │
│  │ /chat/*    │ │ /book/*    │ │ /admin/*   │ │ /ws/chat   │  │
│  └────────────┘ └────────────┘ └────────────┘ └─────────────┘  │
└─────────────────────────────────────────────────────────────────┘
          │                  │                     │
          ▼                  ▼                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                       业务服务层 (Core)                          │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                   对话管理引擎                          │    │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐  │    │
│  │  │ 意图识别    │  │ 多轮上下文  │  │ 响应生成/路由   │  │    │
│  │  └─────────────┘  └─────────────┘  └─────────────────┘  │    │
│  └─────────────────────────────────────────────────────────┘    │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌─────────────┐  │
│  │ RAG 服务   │ │ 馆藏服务   │ │ 用户服务   │ │ 座位服务    │  │
│  └────────────┘ └────────────┘ └────────────┘ └─────────────┘  │
└─────────────────────────────────────────────────────────────────┘
          │                  │                     │
          ▼                  ▼                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                       AI / 数据层                                │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                    RAG 管道                              │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │    │
│  │  │ 文档分块 │→│ Embedding│→│ 向量检索  │→│ 重排序  │ │    │
│  │  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │    │
│  └─────────────────────────────────────────────────────────┘    │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌─────────────┐  │
│  │ 向量数据库 │ │ 关系数据库 │ │ LLM 服务   │ │ 缓存(Redis) │  │
│  │ (Chroma)  │ │ (MySQL)    │ │ (API调用)  │ │             │  │
│  └────────────┘ └────────────┘ └────────────┘ └─────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 二、功能需求（增强版）

### 2.1 典型用户角色

| 角色 | 特点 | 核心诉求 | 技术要点 |
|------|------|----------|----------|
| 在校学生 | 高频使用，移动优先 | 快速查书、续借、预约 | 移动端适配、快捷操作 |
| 教师/科研人员 | 专业查询 | 文献检索、数据库访问 | 精确检索、学术资源导航 |
| 校外访客 | 不熟悉规则 | 开放时间、办证指引 | RAG知识库精准匹配 |
| 图书馆馆员 | 后台管理 | 问题分流、数据统计 | 管理后台、未识别问题聚类 |

### 2.2 核心场景与功能矩阵

| 场景 | 用户输入示例 | 处理流程 | 响应类型 |
|------|-------------|----------|----------|
| **馆藏检索** | "《三体》有没有在架？" | 意图识别 → 馆藏API查询 → 格式化返回 | 卡片(含位置) |
| **借阅管理** | "帮我续借一下" | 身份认证 → 查询借阅 → 二次确认 → 执行续借 | 文本+确认按钮 |
| **座位预约** | "预约明天下午3点的3楼自习室" | 查询可用 → 冲突检测 → 预约确认 | 卡片+确认按钮 |
| **政策FAQ** | "寒暑假借书规则？" | RAG检索 → 知识库匹配 → LLM生成回答 | 富文本+引用来源 |
| **资源引导** | "如何下载知网论文？" | RAG检索 → 分步指导 → 附件链接 | 步骤卡片 |
| **人工转接** | "我要投诉" | 触发关键词 → 排队入队 → 通知转接 | 文本+排队信息 |

---

## 三、RAG 架构详细设计（面试重点）

### 3.1 RAG 管道流程

```
用户问题
    │
    ▼
┌─────────────────────────────────────────────┐
│  Step 1: 查询预处理                          │
│  - 文本清洗（去除特殊字符）                   │
│  - 意图识别（区分"业务查询" vs "知识问答"）  │
│  - 查询改写（可选：用LLM优化检索query）      │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  Step 2: 向量检索                            │
│  - 使用相同Embedding模型编码用户问题         │
│  - 从向量数据库中检索Top-K相关文档块         │
│  - 元数据过滤（如按分类、时间过滤）          │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  Step 3: 重排序 (Rerank)                     │
│  - 使用Cross-Encoder对检索结果重排序        │
│  - 提高相关性，减少无关内容                  │
│  - 输出Top-N最终结果（如Top-3）              │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  Step 4: 上下文构建                          │
│  - 将检索到的文档块拼接为上下文              │
│  - 添加系统Prompt（角色设定、回答规则）      │
│  - 拼接多轮对话历史（最近N轮）              │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  Step 5: LLM 生成                           │
│  - 调用LLM API（支持流式输出SSE）            │
│  - 约束回答基于检索内容，减少幻觉            │
│  - 返回生成结果+引用来源                     │
└─────────────────────────────────────────────┘
```

### 3.2 关键设计决策

#### 3.2.1 文档分块策略 (Chunking)
```python
# 针对不同类型的文档采用不同的分块策略
CHUNK_STRATEGY = {
    "policy_doc": {  # 政策文件
        "method": "recursive_split",
        "chunk_size": 500,      # 每块约500字符
        "chunk_overlap": 100,   # 重叠100字符，保留上下文
        "separator": ["\n##", "\n###", "\n", "。", "！", "？"]
    },
    "faq_doc": {  # FAQ文档
        "method": "qa_pair",    # 按问答对分块
        "chunk_size": 300,
        "chunk_overlap": 0
    },
    "guide_doc": {  # 使用指南
        "method": "section_split",  # 按章节分块
        "chunk_size": 800,
        "chunk_overlap": 150
    }
}
```

#### 3.2.2 Embedding 模型选择

| 模型 | 维度 | 优点 | 缺点 | 适用场景 |
|------|------|------|------|----------|
| **bge-large-zh** | 1024 | 中文效果好，开源免费 | 模型较大(~1.3GB) | 推荐用于中文FAQ |
| **text-embedding-3-small** | 1536 | 调用简单，效果稳定 | 付费，需网络 | 快速开发 |
| **m3e-base** | 768 | 轻量级，速度快 | 效果略逊 | 边缘设备部署 |

**推荐**：`BAAI/bge-large-zh-v1.5`（中文优化，可本地部署）

#### 3.2.3 向量数据库对比

| 特性 | Chroma | FAISS | Milvus | Qdrant |
|------|--------|-------|--------|--------|
| 部署复杂度 | ⭐ 简单 | ⭐ 嵌入 | ⭐⭐⭐ 复杂 | ⭐⭐ 中等 |
| 语义检索 | ✅ | ❌ 需封装 | ✅ | ✅ |
| 元数据过滤 | ✅ | ❌ | ✅ | ✅ |
| 持久化 | ✅ | ❌ 需实现 | ✅ | ✅ |
| 适用规模 | 小(<10万) | 小-中 | 大(>百万) | 中-大 |

**推荐**：`Chroma`（MVP阶段简单快速，支持元数据过滤和持久化）

#### 3.2.4 检索策略

```python
# 混合检索策略示例
RETRIEVAL_CONFIG = {
    "method": "hybrid",  # 混合检索
    "vector_search": {
        "top_k": 20,          # 初步检索数量
        "similarity_threshold": 0.5,  # 最低相似度阈值
        "metadata_filters": {  # 元数据过滤
            "category": ["借阅规则", "开放时间"],  # 按分类过滤
            "updated_at": "2024-01-01"  # 按时间过滤
        }
    },
    "rerank": {
        "model": "bge-reranker-large",
        "top_n": 3,           # 重排序后保留数量
        "min_score": 0.7      # 最低重排序分数
    }
}
```

### 3.3 Prompt 工程设计

#### 3.3.1 系统 Prompt
```python
SYSTEM_PROMPT = """你是一个专业、友善的图书馆智能客服助手。请严格遵循以下规则：

## 角色定位
- 你是图书馆的服务助手，只能回答与图书馆相关的问题
- 回答要简洁、准确、友好，使用简体中文

## 回答规则
1. **优先使用检索内容**：所有回答必须基于提供的参考文档，不要编造信息
2. **不确定性处理**：如果参考文档中没有相关信息，明确告知用户"抱歉，我暂时无法回答这个问题，建议咨询图书馆总服务台"
3. **引用来源**：在回答末尾标注信息来源，格式为 [来源：文档名称]
4. **格式规范**：
   - 操作步骤使用有序列表
   - 重要信息使用加粗
   - 提供具体数字和时间要精确

## 禁止行为
- 不要回答与图书馆无关的问题
- 不要编造不存在的规定或数据
- 不要提供个人隐私信息

## 对话历史
{chat_history}

## 参考文档
{context}

## 用户问题
{question}
"""
```

#### 3.3.2 上下文构建
```python
def build_context(retrieved_docs: list, chat_history: list, question: str) -> str:
    """构建RAG上下文"""
    context_parts = []
    
    # 1. 拼接检索到的文档
    for i, doc in enumerate(retrieved_docs, 1):
        context_parts.append(f"[文档{i}] {doc.content}\n来源：{doc.metadata['source']}")
    
    context_str = "\n\n".join(context_parts)
    
    # 2. 格式化对话历史（仅保留最近5轮）
    history_str = format_chat_history(chat_history[-10:])
    
    # 3. 填充系统Prompt模板
    return SYSTEM_PROMPT.format(
        chat_history=history_str,
        context=context_str,
        question=question
    )
```

---

## 四、API 接口设计（FastAPI）

### 4.1 接口总览

| 模块 | 路径 | 方法 | 功能 |
|------|------|------|------|
| **对话** | `/api/v1/chat` | POST | 发送消息（普通） |
| **对话** | `/api/v1/chat/stream` | POST | 发送消息（流式SSE） |
| **对话** | `/api/v1/chat/history/{session_id}` | GET | 获取会话历史 |
| **对话** | `/api/v1/chat/sessions` | GET | 获取会话列表 |
| **业务** | `/api/v1/book/search` | GET | 馆藏检索 |
| **业务** | `/api/v1/user/borrows` | GET | 查询借阅 |
| **业务** | `/api/v1/user/renew` | POST | 续借图书 |
| **业务** | `/api/v1/seats/availability` | GET | 座位余量 |
| **业务** | `/api/v1/seats/reserve` | POST | 预约座位 |
| **管理** | `/api/v1/admin/kb/documents` | POST | 上传知识库文档 |
| **管理** | `/api/v1/admin/stats/overview` | GET | 数据统计概览 |
| **管理** | `/api/v1/admin/stats/hot-questions` | GET | 热门问题TOP-N |

### 4.2 核心接口详细设计

#### 4.2.1 发送消息（流式）
```
POST /api/v1/chat/stream
Content-Type: application/json

Request Body:
{
    "session_id": "string (可选，新建会话不传)",
    "message": "string (必填，用户消息)",
    "user_id": "string (可选，登录用户传)",
    "metadata": {  // 可选元数据
        "source": "web|wechat|terminal",
        "client_version": "1.0.0"
    }
}

Response (SSE - Server-Sent Events):
event: token
data: {"content": "你"}

event: token
data: {"content": "好"}

event: token
data: {"content": "！"}

event: source
data: {"documents": [{"title": "借阅规则", "chunk_id": "xxx"}]}

event: done
data: {"session_id": "xxx", "usage": {"prompt_tokens": 500, "completion_tokens": 100}}

event: error
data: {"code": "UNSUPPORTED", "message": "不支持的问题类型"}
```

#### 4.2.2 馆藏检索
```
GET /api/v1/book/search?q={keyword}&page={page}&size={size}

Query Parameters:
- q: 搜索关键词（书名/作者/ISBN）
- page: 页码，默认1
- size: 每页数量，默认10

Response (200 OK):
{
    "code": 0,
    "message": "success",
    "data": {
        "total": 5,
        "books": [
            {
                "book_id": "B001",
                "title": "三体",
                "author": "刘慈欣",
                "isbn": "978-7-5366-9293-0",
                "locations": [
                    {
                        "floor": 3,
                        "area": "A区",
                        "shelf": "23架",
                        "available": 2,
                        "total": 5
                    }
                ],
                "status": "available|unavailable|partial"
            }
        ]
    }
}
```

#### 4.2.3 续借图书
```
POST /api/v1/user/renew
Content-Type: application/json
Authorization: Bearer {token}

Request Body:
{
    "user_id": "2024001",
    "book_id": "B001",
    "confirm": false  // 首次请求false，二次确认true
}

Response (200 OK) - 首次请求:
{
    "code": 0,
    "message": "需要确认",
    "data": {
        "need_confirm": true,
        "book_title": "三体",
        "new_due_date": "2026-08-22",
        "message": "确认续借《三体》？续借后到期日将延长30天。"
    }
}

Response (200 OK) - 确认后:
{
    "code": 0,
    "message": "续借成功",
    "data": {
        "book_title": "三体",
        "new_due_date": "2026-08-22",
        "renew_count": 1
    }
}
```

#### 4.2.4 上传知识库文档
```
POST /api/v1/admin/kb/documents
Content-Type: multipart/form-data
Authorization: Bearer {admin_token}

Request Body:
- file: 文档文件（支持 .txt, .md, .pdf, .docx）
- category: 文档分类（如 "借阅规则", "开放时间"）
- description: 文档描述（可选）
- chunk_strategy: 分块策略（可选，默认自动检测）

Response (200 OK):
{
    "code": 0,
    "message": "上传成功",
    "data": {
        "document_id": "DOC001",
        "file_name": "借阅规则手册.pdf",
        "chunks_created": 45,
        "embedding_status": "completed",
        "indexed_at": "2026-08-22T10:30:00Z"
    }
}
```

### 4.3 数据模型定义（Pydantic）

```python
from pydantic import BaseModel, Field
from typing import Optional, List, Dict, Any
from datetime import datetime
from enum import Enum

# ============ 请求模型 ============

class ChatMessage(BaseModel):
    """对话消息请求"""
    session_id: Optional[str] = Field(None, description="会话ID，新会话不传")
    message: str = Field(..., min_length=1, max_length=2000, description="用户消息")
    user_id: Optional[str] = Field(None, description="用户ID")
    metadata: Optional[Dict[str, Any]] = Field(default_factory=dict)

class BookSearchRequest(BaseModel):
    """图书检索请求"""
    keyword: str = Field(..., min_length=1, description="搜索关键词")
    search_type: str = Field("auto", pattern="^(auto|title|author|isbn)$")
    page: int = Field(1, ge=1)
    page_size: int = Field(10, ge=1, le=50)

class RenewRequest(BaseModel):
    """续借请求"""
    user_id: str
    book_id: str
    confirm: bool = Field(False, description="二次确认标志")

class SeatReserveRequest(BaseModel):
    """座位预约请求"""
    user_id: str
    area: str = Field(..., description="区域名称")
    date: str = Field(..., pattern=r"^\d{4}-\d{2}-\d{2}$")
    start_time: str = Field(..., pattern=r"^\d{2}:\d{2}$")
    end_time: str = Field(..., pattern=r"^\d{2}:\d{2}$")

# ============ 响应模型 ============

class ApiResponse(BaseModel):
    """统一响应封装"""
    code: int = Field(0, description="0=成功, 非0=错误")
    message: str = Field("success")
    data: Optional[Any] = None

class BookInfo(BaseModel):
    """图书信息"""
    book_id: str
    title: str
    author: str
    isbn: str
    locations: List[Dict[str, Any]]
    status: str  # available, unavailable, partial

class ChatReply(BaseModel):
    """对话响应"""
    reply_type: str  # text, card, image, link, confirm, transfer
    content: str
    extra: Optional[Dict[str, Any]] = None
    sources: Optional[List[Dict[str, str]]] = None  # 引用来源
```

---

## 五、数据库设计

### 5.1 关系数据库表结构（MySQL）

```sql
-- 用户表
CREATE TABLE users (
    user_id VARCHAR(32) PRIMARY KEY,
    username VARCHAR(50) NOT NULL,
    user_type ENUM('student', 'teacher', 'visitor', 'admin'),
    token_hash VARCHAR(128),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME ON UPDATE CURRENT_TIMESTAMP
);

-- 会话表
CREATE TABLE sessions (
    session_id VARCHAR(32) PRIMARY KEY,
    user_id VARCHAR(32),
    title VARCHAR(100),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id)
);

-- 消息表
CREATE TABLE messages (
    message_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    session_id VARCHAR(32) NOT NULL,
    role ENUM('user', 'assistant', 'system') NOT NULL,
    content TEXT NOT NULL,
    token_count INT,
    sources JSON,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_session_id (session_id),
    INDEX idx_created_at (created_at)
);

-- 知识库文档表
CREATE TABLE knowledge_documents (
    doc_id VARCHAR(32) PRIMARY KEY,
    file_name VARCHAR(255) NOT NULL,
    file_type VARCHAR(20),
    category VARCHAR(50),
    chunk_count INT DEFAULT 0,
    status ENUM('pending', 'processing', 'completed', 'failed'),
    uploaded_by VARCHAR(32),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME ON UPDATE CURRENT_TIMESTAMP
);

-- 未解决问题表（用于持续改进）
CREATE TABLE unresolved_questions (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    question TEXT NOT NULL,
    session_id VARCHAR(32),
    user_id VARCHAR(32),
    intent_predicted VARCHAR(50),
    resolved TINYINT DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_resolved (resolved),
    INDEX idx_created_at (created_at)
);

-- 操作日志表
CREATE TABLE operation_logs (
    log_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id VARCHAR(32),
    action VARCHAR(50) NOT NULL,
    target_type VARCHAR(50),
    target_id VARCHAR(32),
    detail JSON,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_action (user_id, action),
    INDEX idx_created_at (created_at)
);
```

### 5.2 向量数据库设计（Chroma）

```python
# Chroma集合配置
COLLECTION_CONFIG = {
    "knowledge_base": {
        "name": "library_knowledge",
        "embedding_model": "BAAI/bge-large-zh-v1.5",
        "metadata_schema": {
            "doc_id": "string",       # 关联文档ID
            "category": "string",     # 分类：借阅规则、开放时间等
            "source": "string",       # 来源文件
            "updated_at": "datetime", # 更新时间
            "priority": "int"         # 优先级（1-5）
        }
    }
}
```

---

## 六、多轮对话管理

### 6.1 会话历史策略

```python
class SessionManager:
    """会话管理器"""
    
    MAX_HISTORY_TURNS = 10  # 最多保留10轮对话
    MAX_HISTORY_TOKENS = 2000  # 历史Token上限
    
    def build_chat_history(self, messages: list) -> str:
        """构建对话历史，控制Token数量"""
        recent_messages = messages[-self.MAX_HISTORY_TURNS:]
        
        # 从最近消息开始，逐步添加直到达到Token上限
        history = []
        total_tokens = 0
        
        for msg in reversed(recent_messages):
            msg_tokens = count_tokens(msg.content)
            if total_tokens + msg_tokens > self.MAX_HISTORY_TOKENS:
                break
            history.insert(0, msg)
            total_tokens += msg_tokens
        
        # 格式化
        formatted = []
        for msg in history:
            role = "用户" if msg.role == "user" else "助手"
            formatted.append(f"{role}：{msg.content}")
        
        return "\n".join(formatted)
```

### 6.2 上下文管理策略

```python
# 上下文窗口分配
CONTEXT_WINDOW = {
    "system_prompt": 500,      # 系统Prompt预留
    "retrieved_docs": 2000,    # 检索内容
    "chat_history": 1500,      # 对话历史
    "user_query": 200,         # 当前用户问题
    "response_budget": 1500    # 响应生成预算
}
```

---

## 七、错误处理与边界场景

### 7.1 错误码规范

| 错误码 | HTTP状态码 | 描述 | 处理策略 |
|--------|-----------|------|----------|
| 0 | 200 | 成功 | - |
| 1001 | 400 | 参数错误 | 返回具体字段错误信息 |
| 1002 | 401 | 未认证 | 引导用户登录 |
| 1003 | 403 | 无权限 | 提示联系管理员 |
| 2001 | 404 | 资源不存在 | 返回友好提示 |
| 3001 | 429 | 请求过于频繁 | 返回重试时间建议 |
| 4001 | 500 | 服务内部错误 | 记录日志，返回通用错误 |
| 4002 | 503 | LLM服务不可用 | 降级到关键词匹配 |
| 4003 | 504 | LLM响应超时 | 返回超时提示，建议重试 |

### 7.2 RAG 边界场景处理

```python
class RAGEdgeCaseHandler:
    """RAG边界场景处理"""
    
    def handle_low_confidence(self, query: str, scores: list) -> dict:
        """低置信度处理"""
        max_score = max(scores) if scores else 0
        
        if max_score < 0.3:
            # 场景1：完全不相关 → 引导转人工
            return {
                "action": "transfer_to_human",
                "message": "抱歉，我暂时无法回答这个问题，建议您咨询图书馆总服务台或输入「人工」转接人工客服。",
                "log": "low_confidence"
            }
        elif max_score < 0.5:
            # 场景2：部分相关 → 给出最接近的答案并标注
            return {
                "action": "answer_with_disclaimer",
                "message": "根据我的理解，您可能想问的是……（以下为最接近的回答，仅供参考）",
                "need_human_followup": True
            }
    
    def handle_multi_intent(self, query: str, intents: list) -> dict:
        """多意图处理"""
        if len(intents) > 1:
            return {
                "action": "ask_clarification",
                "message": "您的问题可能涉及多个方面，请问您主要想了解哪一项？",
                "options": intents
            }
    
    def handle_out_of_scope(self, query: str) -> dict:
        """超出知识库范围"""
        out_of_scope_topics = [
            "天气预报", "新闻", "娱乐", "体育",
            "编程问题", "数学题", "翻译请求"
        ]
        if any(topic in query for topic in out_of_scope_topics):
            return {
                "action": "reject_politely",
                "message": "抱歉，我是图书馆专用客服，只能回答与图书馆相关的问题。"
            }
```

---

## 八、评测指标体系

### 8.1 RAG 评测指标

| 指标 | 计算方法 | 目标值 | 说明 |
|------|----------|--------|------|
| **检索召回率** | 相关文档数/总相关文档数 | ≥90% | 衡量检索的完整性 |
| **检索精准率** | 相关文档数/检索文档总数 | ≥80% | 衡量检索的准确性 |
| **答案准确率** | 人工评判正确数/总问题数 | ≥95% | 最终答案的正确性 |
| **幻觉率** | 编造信息次数/总回答次数 | ≤2% | LLM编造信息的比例 |
| **引用准确率** | 正确引用数/总引用数 | ≥98% | 引用来源的准确性 |
| **无答案率** | 无答案次数/总请求数 | ≤10% | 应该给出答案却没有的比例 |

### 8.2 系统性能指标

| 指标 | 目标值 | 测试方法 |
|------|--------|----------|
| 平均响应时间（检索） | ≤500ms | 1000次请求取平均 |
| 平均响应时间（LLM生成） | ≤3s | 流式输出首Token≤500ms |
| 并发支持 | ≥200 QPS | 压力测试 |
| 系统可用性 | ≥99.9% | 月度统计 |

### 8.3 业务指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 自助解决率 | ≥75% | 无需转人工的比例 |
| 用户满意度 | ≥4.5/5 | 5分制评分 |
| 重复问题率 | ≤20% | 相同问题重复询问的比例 |
| 人工转接率 | ≤25% | 转接人工的比例 |

---

## 九、安全设计

### 9.1 API 安全措施

```python
SECURITY_CONFIG = {
    "authentication": {
        "method": "JWT + Refresh Token",
        "access_token_ttl": 3600,  # 1小时
        "refresh_token_ttl": 604800  # 7天
    },
    "rate_limiting": {
        "per_user": 30,       # 每用户每分钟30次
        "per_ip": 100,        # 每IP每分钟100次
        "burst_allowance": 50  # 突发允许50次
    },
    "input_validation": {
        "max_message_length": 2000,
        "max_file_size": 10485760,  # 10MB
        "injection_patterns": [
            r"<script.*?>",
            r"javascript:",
            r"(DROP|DELETE|INSERT)\s+(TABLE|INTO)",
            r"\.\./"  # 路径遍历
        ]
    },
    "privacy_protection": {
        "mask_phone": True,     # 手机号脱敏
        "mask_id_card": True,   # 身份证脱敏
        "mask_student_id": False  # 学号可完整显示
    }
}
```

### 9.2 数据脱敏规则

```python
def mask_sensitive_data(text: str, data_type: str) -> str:
    """敏感数据脱敏"""
    rules = {
        "phone": lambda x: x[:3] + "****" + x[-4:],
        "id_card": lambda x: x[:6] + "********" + x[-4:],
        "email": lambda x: x[:2] + "***" + x[x.index("@"):]
    }
    if data_type in rules:
        return rules[data_type](text)
    return text
```

---

## 十、部署与运维

### 10.1 项目结构

```
LibrarySmartService/
├── app/
│   ├── __init__.py
│   ├── main.py                  # FastAPI应用入口
│   ├── config.py                # 配置管理
│   ├── dependencies.py          # 依赖注入
│   │
│   ├── api/                     # API路由层
│   │   ├── __init__.py
│   │   ├── v1/
│   │   │   ├── __init__.py
│   │   │   ├── chat.py          # 对话接口
│   │   │   ├── book.py          # 图书接口
│   │   │   ├── user.py          # 用户接口
│   │   │   ├── seat.py          # 座位接口
│   │   │   └── admin.py         # 管理接口
│   │   │
│   ├── services/                # 业务服务层
│   │   ├── __init__.py
│   │   ├── chat_service.py      # 对话服务
│   │   ├── rag_service.py       # RAG服务
│   │   ├── book_service.py      # 馆藏服务
│   │   ├── user_service.py      # 用户服务
│   │   ├── seat_service.py      # 座位服务
│   │   ├── knowledge_service.py # 知识库服务
│   │   │
│   ├── rag/                     # RAG管道
│   │   ├── __init__.py
│   │   ├── document_loader.py   # 文档加载器
│   │   ├── text_chunker.py      # 文本分块器
│   │   ├── embeddings.py        # Embedding服务
│   │   ├── vector_store.py      # 向量存储
│   │   ├── retriever.py         # 检索器
│   │   ├── reranker.py          # 重排序器
│   │   └── prompt_builder.py    # Prompt构建器
│   │   │
│   ├── models/                  # 数据模型
│   │   ├── __init__.py
│   │   ├── schemas.py           # Pydantic模型
│   │   └── database.py          # SQLAlchemy模型
│   │   │
│   ├── core/                    # 核心组件
│   │   ├── __init__.py
│   │   ├── session_manager.py   # 会话管理
│   │   ├── llm_client.py        # LLM客户端
│   │   ├── cache.py             # 缓存管理
│   │   └── logger.py            # 日志配置
│   │   │
│   └── utils/                   # 工具函数
│       ├── __init__.py
│       ├── validators.py        # 验证器
│       └── helpers.py           # 辅助函数
│
├── data/                        # 数据目录
│   ├── knowledge/               # 知识库原文
│   ├── chroma/                  # 向量数据库
│   └── backups/                 # 备份
│
├── tests/                       # 测试
│   ├── test_chat.py
│   ├── test_rag.py
│   └── test_api.py
│
├── docs/                        # 文档
│   └── api_spec.yaml            # OpenAPI规范
│
├── requirements.txt
├── Dockerfile
├── docker-compose.yml
├── .env.example
└── README.md
```

### 10.2 核心依赖

```txt
# requirements.txt

# Web框架
fastapi==0.110.0
uvicorn==0.29.0
pydantic==2.6.4

# 数据库
sqlalchemy==2.0.29
pymysql==1.1.0
redis==5.0.3

# RAG相关
chromadb==0.5.0
sentence-transformers==2.7.0
transformers==4.40.0
torch==2.2.2
pypdf==4.2.0
python-docx==1.1.0

# LLM调用
httpx==0.27.0

# 其他
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
python-dotenv==1.0.1
loguru==0.7.2
pytest==8.1.1
```

### 10.3 配置文件示例

```env
# .env.example

# 服务配置
APP_NAME=LibrarySmartService
APP_VERSION=2.0.0
DEBUG=true
HOST=0.0.0.0
PORT=8000

# 数据库配置
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DB=library_service
MYSQL_USER=root
MYSQL_PASSWORD=your_password

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# LLM配置
LLM_PROVIDER=your_llm_provider
LLM_API_KEY=your_api_key
LLM_MODEL=your_model_name
LLM_BASE_URL=https://api.your-provider.com/v1

# Embedding配置
EMBEDDING_MODEL=BAAI/bge-large-zh-v1.5
EMBEDDING_DEVICE=cpu

# Chroma配置
CHROMA_PERSIST_DIR=./data/chroma

# 安全配置
JWT_SECRET_KEY=your_secret_key
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# 日志配置
LOG_LEVEL=INFO
LOG_DIR=./logs
```



## 十、实施路线图

### Phase 1: MVP（2-4周）
- [x] 需求分析（本文档）
- [ ] 项目脚手架搭建
- [ ] 基础CRUD接口
- [ ] 简单关键词匹配FAQ
- [ ] Docker部署

### Phase 2: RAG集成（3-4周）
- [ ] 知识库文档准备
- [ ] 文档分块与向量化
- [ ] Chroma向量数据库集成
- [ ] Embedding模型部署
- [ ] 基础RAG管道实现

### Phase 3: 业务集成（3-4周）
- [ ] LLM API集成
- [ ] 多轮对话管理
- [ ] 馆藏系统对接（Mock）
- [ ] 用户认证集成
- [ ] 座位预约对接（Mock）

### Phase 4: 优化上线（2-3周）
- [ ] Prompt优化
- [ ] 性能调优
- [ ] 错误处理完善
- [ ] 安全加固
- [ ] 压力测试
- [ ] 前端对接

---

## 附录：面试高频问题速查

### RAG 相关
1. **Q**: 如何选择合适的分块大小？
   **A**: 经验法则是 300-1000 字符，根据文档类型调整。政策文档可用较大分块，FAQ应用较小分块。关键是保留语义完整性。

2. **Q**: 如何降低 RAG 中的幻觉率？
   **A**: 
   - 检索阶段：提高召回率（增加Top-K）、优化Embedding模型
   - 重排序：使用 Cross-Encoder 精排
   - 生成阶段：严格 Prompt 约束、引用溯源、设置置信度阈值
   - 后处理：检查答案是否在检索内容中

3. **Q**: Chroma 和 FAISS 的区别？
   **A**: Chroma 是完整的向量数据库，支持元数据过滤、持久化；FAISS 是向量索引库，需要自行封装存储和检索逻辑。Chroma 适合快速开发，FAISS 适合定制化需求。

### FastAPI 相关
4. **Q**: FastAPI 的依赖注入是如何实现的？
   **A**: 使用 `Depends()` 声明依赖，FastAPI 会自动解析并注入。支持嵌套依赖、生成器依赖（用于数据库会话管理）。

5. **Q**: 如何实现 SSE 流式响应？
   **A**: 使用 `StreamingResponse`，通过 `yield` 逐块返回数据，设置 `media_type="text/event-stream"`，每个事件以 `data: ` 开头，以 `\n\n` 结尾。

6. **Q**: Pydantic v2 相比 v1 有什么改进？
   **A**: 性能提升 5-50 倍、更好的序列化支持、`@computed_field`、`model_validator` 替代 `root_validator`。

---

> **文档维护者**：图书馆智能客服项目组
> **最后更新**：2026-08-22
