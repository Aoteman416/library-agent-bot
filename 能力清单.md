# 图书馆智能客服 - 系统能力清单

> 版本：v2.0.0 | 更新日期：2026-08-22
> 本清单描述本系统具备的全部业务能力、技术能力与工程能力，可用于学习路线规划、面试准备、功能验收。

---

## 一、业务能力（面向用户）

### 1.1 智能对话（核心）
| 能力项 | 说明 |
|---|---|
| 自然语言问答 | 用户可直接用自然语言提问，系统自动识别意图并返回答案 |
| SSE 流式回答 | 回答逐字/逐块打字机效果输出，支持实时接收 token 事件 |
| 多轮上下文 | 跨轮次保留对话历史，支持「这本书」「那本」等指代消解 |
| 查询改写 | 结合上下文对用户问题进行语义改写，消除歧义 |
| 快捷问题 | 首页提供常用问题 chips，点击即发送（查书、开放时间、座位、知网、查重） |

### 1.2 三通道意图路由
系统自动将问题分发到三条处理通道，优先级：业务 > FAQ 快速 > RAG 兜底。

| 通道 | 包含意图 | 典型问题 |
|---|---|---|
| **业务通道** | book_search、borrow_manage、seat_reserve、human_transfer | 查书、续借、预约座位、转人工 |
| **FAQ 快速通道** | policy_faq、resource_guide、navigation | 开放时间、借阅规则、知网怎么用、服务台在哪 |
| **RAG 兜底通道** | unknown / 低置信度问题 | 任意未命中上述类别的问题 |

### 1.3 图书检索（book_search）
| 能力项 | 说明 |
|---|---|
| 关键词模糊匹配 | 按书名 / 作者 / ISBN 三字段检索 |
| 口语化清洗 | 自动去除「有没有在架」「请问」「吗」等填充词 |
| 图书状态判定 | 可借 / 部分可借 / 不可借三档，基于 available/total |
| 卡片式返回 | 返回标题 + 作者 + ISBN + 位置 + 馆藏 + 状态字段 |
| Mock 数据 5 册 | 《三体》《C++ Primer 第六版》《Python 编程：从入门到实践》《深度学习》《机器学习》 |

### 1.4 借阅管理（borrow_manage）
| 能力项 | 说明 |
|---|---|
| 借阅查询 | 展示当前在借书籍列表、到期日、已续借次数、欠费金额 |
| 续借（多轮槽位填充） | 缺少书名时主动追问，支持从历史对话中识别《书名》 |
| 续借次数限制 | 每本书最多续借 1 次，超限提示 |
| 续借二次确认 | 填完槽位后要求用户回复「确认」/「取消」，5 分钟超时 |
| 到期日自动计算 | 续借成功后在原到期日基础上 +30 天 |

### 1.5 座位预约（seat_reserve）
| 能力项 | 说明 |
|---|---|
| 座位余量查询 | 返回 3 楼自习室、4 楼自习室、5 楼研习间的总数/可用数 |
| 槽位 4 要素 | 区域、日期、开始时间、结束时间，逐轮追问补齐 |
| 自然语言时间解析 | 支持「下午 3 点」「明天」「2026-08-23」「2 点到 4 点」等 |
| 时间段默认值 | 单时间点默认预约 2 小时 |
| 冲突检测 | 同一用户当日同区域不可重复预约 |
| 二次确认 + 超时 | 填完信息后确认操作，5 分钟内有效 |
| 预约取消 | 通过预约编号取消 |

### 1.6 图书馆规则 FAQ（policy_faq）
| 能力项 | 说明 |
|---|---|
| 开放时间问答 | 工作日 / 周末 / 寒暑假闭馆安排 |
| 办证流程 | 办卡材料、地点、费用、挂失补办 |
| 借阅规则 | 最大册数、借期、续借规则、超期罚款 |
| 资源赔偿 | 丢失 / 污损赔偿条款 |
| 入馆须知 | 能否带咖啡、书包、打印纸等 |

### 1.7 电子资源指南（resource_guide）
| 能力项 | 说明 |
|---|---|
| 数据库使用 | 知网、万方、Web of Science 等访问入口与校外 VPN |
| 论文查重 | 图书馆免费查重名额、流程、限制 |
| 自助打印 | 打印机位置、费用、APP 操作 |
| 电子资源访问 | 馆外访问 VPN 配置方法 |

### 1.8 馆内导航（navigation）
| 能力项 | 说明 |
|---|---|
| 功能区位置 | 总服务台、还书箱、自习室、厕所、饮水处 |
| 楼层布局 | 1-5 楼功能分布简介 |

### 1.9 人工转接（human_transfer）
| 能力项 | 说明 |
|---|---|
| 转人工客服 | 返回排队位置、预计等待时间 |
| 夜间降级 | 非工作时间自动转留言工单模式 |

### 1.10 用户体系
| 能力项 | 说明 |
|---|---|
| 匿名游客模式 | 默认身份，可使用检索、FAQ、座位余量等公共能力 |
| 演示登录 | 学号 2024001 / 2024002，密码 123456 |
| 登录升级流程 | 匿名触发需登录操作时自动引导登录，登录成功后恢复原请求 |
| 退出登录 | 一键切回游客，清除本地学号 |
| 演示账号角色 | 本科生、研究生（可扩展管理员） |

### 1.11 反馈与评价
| 能力项 | 说明 |
|---|---|
| 满意度评分 | 1-5 星评分 + 文字建议 |
| 是否有帮助 | 布尔型反馈，用于知识库回流 |
| 反馈统计 | 平均分、好评率、低分数量 |

---

## 二、RAG 知识库能力

### 2.1 文档加载
| 能力项 | 代码位置 |
|---|---|
| 目录自动扫描 | `KnowledgeService.load_directory()` 读取 `data/knowledge/*.md`、`*.txt` |
| Markdown 解析 | 按 `#` 一级 / `##` 二级标题切分为章节，标题作为 category 元数据 |
| 知识文档 4 份 | 借阅规则.md、开放时间与办证.md、资源使用.md、馆内设施与导航.md |

### 2.2 文本分块（Chunker）
| 能力项 | 说明 |
|---|---|
| 三级递归切分 | 段落 → 句子 → 字符窗口 |
| 重叠窗口 | chunk_size 默认 200，chunk_overlap 默认 50 |
| 超长句硬切 | 单句超出窗口时按步长 (size-overlap) 硬切 |
| 元数据携带 | source（文件名）、category（章节标题） |

### 2.3 Embedding 嵌入
| 能力项 | 说明 |
|---|---|
| 字符 n-gram TF-IDF | 纯 Python 实现，零外部依赖 |
| 特征项 | 单字 + 相邻双字，过滤停用词 |
| 稀疏向量 | `{term: tf_idf_weight}` 字典格式 |
| 余弦相似度 | 稀疏向量点积 / 范数乘积 |
| 可切换模式 | `EMBEDDING_MODE=ngram`（默认） / `external`（预留 sentence-transformers） |

### 2.4 向量存储（VectorStore）
| 能力项 | 说明 |
|---|---|
| 内存索引 | 基于列表的 Chunk + Vector 一一映射 |
| 元数据过滤 | 检索时可按 category / source 过滤 |
| Top-K 检索 | 按相似度降序返回前 K 条 |
| 持久化接口 | `to_json()` / `load_json()` 序列化 |
| 可替换性 | 接口对齐 Chroma / FAISS / Milvus，便于后续替换 |

### 2.5 检索器（Retriever）
| 能力项 | 说明 |
|---|---|
| 两阶段检索 | 阶段一：向量 Top-K 初检（默认 10）；阶段二：重排 Top-N（默认 3） |
| 重排公式 | `final = 0.75 × cosine + 0.25 × 词项覆盖率` |
| 词项覆盖率 | query 分词与文档分词的 Jaccard 交集占比 |

### 2.6 答案生成（双模式）
| 模式 | 触发条件 | 行为 |
|---|---|---|
| **LLM 模式** | 配置了 `LLM_API_KEY` 且调用成功 | 通过 prompt_builder 组装 system/user/knowledge 消息，调用 LLM 生成 |
| **离线模板模式** | 未配置 Key / LLM 调用失败 | 用检索到的 Top-N 知识块编号拼接，始终有知识库支撑 |
| **低置信度兜底** | 无检索结果 / 最高分 < 0.15 | 给出「未找到信息」+ 3 条建议（换关键词 / 转人工 / 官网） |

### 2.7 Prompt 构建
| 能力项 | 说明 |
|---|---|
| 角色注入 | System Prompt 设定「你是图书馆智能客服」 |
| 知识注入 | 将 Top-N 检索内容编号后作为参考资料插入 |
| 历史注入 | 最近若干轮对话用于维持多轮一致性 |
| 防幻觉指令 | 明确要求「只基于参考资料回答，不确定时说明」 |

### 2.8 FAQ 语义缓存
| 能力项 | 说明 |
|---|---|
| 两级命中 | 精确文本匹配 → 向量语义匹配（阈值 0.85） |
| LRU 容量 | 默认 500 条，超量淘汰最久未使用 |
| TTL 过期 | 默认 3600 秒，过期自动失效 |
| 命中率监控 | hit / miss / 命中率统计接口 |

---

## 三、对话管理（NLU / DST）能力

### 3.1 意图分类（IntentClassifier）
| 能力项 | 说明 |
|---|---|
| 加权关键词分类 | 7 类意图 × 关键词权重表，命中求和取最高 |
| 意图列表 | human_transfer、book_search、borrow_manage、seat_reserve、policy_faq、resource_guide、navigation |
| 冲突消解 | 业务与 FAQ 双命中时，次高分 ≥ 0.8 倍最高分则优先 FAQ |
| 未知意图 | 无命中时走 RAG 通道 |

### 3.2 自然语言解析（NLU）
| 能力项 | 说明 |
|---|---|
| 时间解析 | `下午3点 / 上午10点半 / 15:30 / 3点(默认下午)` → `HH:MM` |
| 日期解析 | `今天 / 明天 / 8月23日 / 2026-08-23` → `YYYY-MM-DD` |
| 区域解析 | 5 个区域关键词词典（3 楼自习室 / 4 楼自习室 / 5 楼研习间 / 2 楼文学区 / 1 楼总服务台） |
| 时间段范围 | `下午2-4点` / `2点到4点` → start_time + end_time |
| 默认时长 | 单时间点默认 +2 小时 |
| 实体抽取入口 | `extract_entities(text)` 一次返回 date / start / end / area |

### 3.3 对话状态追踪（DST）
| 能力项 | 说明 |
|---|---|
| 状态机阶段 | `idle → collecting_slots → confirming → completed` |
| 槽位模板 | 座位预约 4 槽、续借 1 槽，可扩展 |
| 混合填充 | 一句话同时提供多个槽位时批量填充 |
| 追问策略 | 按槽位顺序依次追问第一个缺失项 |
| 二次确认机制 | `set_pending_confirm → confirm / cancel`，5 分钟 TTL |
| 幂等键 | confirm_id 防重复执行 |

### 3.4 查询改写（QueryRewriter）
| 能力项 | 说明 |
|---|---|
| 指代消解 | 结合历史识别「这本书 / 那个 / 它」等指代词 |
| 上下文补全 | 历史命中《书名》时自动注入 |

---

## 四、安全与权限能力

### 4.1 Prompt 注入防护（SecurityGuard）
| 能力项 | 说明 |
|---|---|
| 指令覆盖检测 | 「忽略以上所有指令」「forget previous instructions」等中/英模式 |
| 数据泄露检测 | 「你的系统提示是什么」「print your prompt」等 |
| 角色切换检测 | 「你现在是…」「jailbreak」「DAN mode」等 |
| 输入长度限制 | 单轮上限 2000 字符 |
| 重复字符检测 | 连续 30+ 相同字符拦截 |
| 审计日志 | 所有拦截行为写入 logger.warning |
| LLM 输出清洗 | 生成结果二次扫描替换注入尝试 |

### 4.2 敏感数据脱敏
| 能力项 | 说明 |
|---|---|
| 身份证号 | 18 位 → `[身份证号]` |
| 手机号 | 1[3-9]xxxxxxxxx → `[手机号]` |
| 密码 | `密码：xxx` / `password=xxx` → `[已隐藏]` |
| 应用场景 | 会话存储、日志输出前自动脱敏 |

### 4.3 权限矩阵（PermissionManager）

| 功能 | 匿名 | 登录用户 | 管理员 |
|---|---|---|---|
| 馆藏检索 | ✅ | ✅ | ✅ |
| 座位余量查询 | ✅ | ✅ | ✅ |
| FAQ / RAG 问答 | ✅ | ✅ | ✅ |
| 借阅查询 / 续借 / 还书 | ❌ | ✅ | ✅ |
| 座位预约 / 取消 | ❌ | ✅ | ✅ |
| 个人信息查询 | ❌ | ✅（本人） | ✅ |
| 管理统计（热门问题/通道分布） | ❌ | ❌ | ✅ |
| 知识库维护（增删/重建索引） | ❌ | ❌ | ✅ |

### 4.4 会话安全
| 能力项 | 说明 |
|---|---|
| 会话超时 | 普通 30 分钟，公共设备 10 分钟无活动自动失效 |
| 公共设备模式 | 请求头 `X-Public-Device: true` 启用，超时后主动清空会话 |
| 会话隔离 | session_id 绑定数据，不会串号 |
| 历史裁剪 | 最近 10 轮（20 条消息），更早消息丢弃以防上下文膨胀 |

---

## 五、后台管理与统计能力

| 能力项 | 说明 |
|---|---|
| 热门问题统计 | 按问题文本计数 Top-N |
| 通道分布统计 | business / faq_fast / rag 三类通道命中次数与占比 |
| 未解决问题收集 | RAG 低置信度问题自动记录，用于知识库补全 |
| 反馈统计 | 总条数、平均分、好评率、差评数 |
| 会话列表 | 按最后活动时间排序，展示 phase / intent / 消息数 / 预览 |
| 会话历史回放 | 按 session_id 查看完整消息流水与阶段信息 |
| 缓存统计 | FAQ 语义缓存命中率、容量使用情况 |
| RAG 索引状态 | 文档分块数、向量库容量 |

---

## 六、工程能力

### 6.1 后端框架
| 能力项 | 说明 |
|---|---|
| FastAPI 异步 | 路由 / 中间件 / lifespan 启动钩子 / Pydantic 校验 |
| CORS 放行 | `allow_origins=*` 便于本地前端调试 |
| SSE 流式响应 | `StreamingResponse` + `text/event-stream` 媒体类型 |
| 健康检查 | `GET /health` 返回版本信息 |
| 静态资源挂载 | `web/` 目录挂载到根路径，前后端同端口部署 |

### 6.2 配置管理
| 能力项 | 说明 |
|---|---|
| `.env` 驱动 | `python-dotenv` 加载，所有运行参数可环境变量覆盖 |
| 参数一览 | APP_NAME / PORT / LLM_* / EMBEDDING_MODE / TOP_K / RERANK_TOP_N / CHUNK_SIZE / CHUNK_OVERLAP |
| 配置中心化 | `app/config.py` 单例 `settings` 对象，全局引用 |

### 6.3 依赖
| 依赖 | 用途 |
|---|---|
| fastapi ≥ 0.110 | Web 框架 |
| uvicorn[standard] ≥ 0.29 | ASGI 服务器 |
| pydantic ≥ 2.6 | 请求/响应 Schema 校验 |
| httpx ≥ 0.27 | LLM 异步 HTTP 客户端 |
| python-dotenv ≥ 1.0 | 环境变量加载 |
| sentence-transformers（可选） | 接入外部 Embedding |
| chromadb（可选） | 接入真实向量库 |
| pypdf / python-docx（可选） | 解析 PDF / Word 知识文档 |
| python-multipart（可选） | 上传文件接口 |

### 6.4 日志与错误处理
| 能力项 | 说明 |
|---|---|
| 结构化日志 | 启动信息、索引构建过程、LLM 降级原因、注入拦截 |
| 接口异常兜底 | SSE 事件流捕获所有异常，以 `error` 事件输出 |
| LLM 自动降级 | Key 缺失、调用超时、网络异常 → 自动回退离线模板 |

### 6.5 测试
| 能力项 | 位置 |
|---|---|
| HTTP 接口测试 | `tests/test_http_v2.py` |
| 意图分类器测试 | `tests/test_intent_classifier.py` |
| 业务逻辑测试 | `tests/test_v2_logic.py` |

### 6.6 前端
| 能力项 | 说明 |
|---|---|
| 原生 HTML + CSS + JS | 零构建，直接打开或由 FastAPI 静态挂载 |
| SSE 流式渲染 | `EventSource` / fetch 流式读取，token 级增量渲染 |
| 登录 / 退出按钮 | localStorage 保存学号，请求头 `X-Demo-User` 携带 |
| 快捷问题 chips | 一键发常见问题 |
| 卡片 / 确认 / 登录提示多种回复类型渲染 | reply_type 分发渲染逻辑 |

---

## 七、可扩展能力（预留但未实现）

| 能力项 | 扩展点位置 |
|---|---|
| 真实 LLM 接入 | `app/core/llm_client.py` 已实现 OpenAI 兼容协议，填入 `LLM_API_KEY` 即可 |
| 真实 Embedding | `app/rag/embeddings.py` 中 `DummyExternalEmbedder` → sentence-transformers |
| 真实向量数据库 | `app/rag/vector_store.py` 接口对齐 Chroma / FAISS，可直接替换 |
| PDF / Word 知识文档 | 安装可选依赖后在 `KnowledgeService.load_file` 扩展解析 |
| 真实 ILS 对接 | `app/services/book_service.py` / `user_service.py` Mock 方法改为 HTTP 调用 OPAC |
| 知识库 Web 管理 | `admin_service` 已有统计，可扩展文件上传 + 增量 reindex 接口 |
| 管理员角色认证 | 请求头 `X-Demo-Role: admin` → 配合权限矩阵即可启用管理接口 |
